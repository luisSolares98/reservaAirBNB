server:
  servlet:
    context-path: /airNUR
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://postgres:5432/msreserva"
    username: postgres
    password: root
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
        ddl-auto: create-drop

spring-doc:
  swagger-ui:
    path: /
