server:
  servlet:
    context-path: /airNUR
  port: ${PORT:8082}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Restaurante
    username: postgres
    password: root
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update

spring-doc:
  swagger-ui:
    path: /
